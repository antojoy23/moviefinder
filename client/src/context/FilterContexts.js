import { createContext } from "react";

export const SearchTermContext = createContext(null);
export const SetSearchTermContext = createContext(null);

export const SearchTypeContext = createContext(null);
export const SetSearchTypeContext = createContext(null);

export const SearchYearContext = createContext(null);
export const SetSearchYearContext = createContext(null);